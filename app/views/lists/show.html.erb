<h1> <%= @list.name %></h1>
<%# <%= link_to 'Update list', edit_list_path %>

  <ul>
    <% @list.bookmarks.each do |bookmark| %>
      <li><%= bookmark.movie.title %>
      <%= bookmark.comment %></li>
      <%= link_to 'Delete a bookmark', bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure"}%>
    <% end %>
  </ul>

<%= link_to 'Delete the list',
  list_path(@list),
  data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}
%>
<%= link_to 'Lists index', lists_path%>
<%= link_to 'Add a movie', new_list_bookmark_path(@list)%>
